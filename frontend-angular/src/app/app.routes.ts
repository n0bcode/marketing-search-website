import { Routes } from '@angular/router';
import { GoogleSearchComponent } from './pages/google-search/google-search.component';

export const routes: Routes = [
  { path: '', component: GoogleSearchComponent },
];
